export const selectToken = (state) => state.user.token

export const selectUser = (state) => state.user

export const selectName = (state) => state.user.name

export const selectUserSummaries = (state) => state.user.summaries
